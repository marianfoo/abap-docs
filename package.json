{
  "name": "abap-docs",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    
    "// === MAIN SCRIPTS ===": "",
    "scrape": "node scripts/scrape.js",
    "scrape:all": "node scripts/scrape.js --all",
    "scrape:758": "node scripts/scrape.js --version 7.58 --force",
    "scrape:latest": "node scripts/scrape.js --version latest --force",
    
    "generate": "node scripts/generate.js",
    "generate:all": "node scripts/generate.js --all",
    "generate:758": "node scripts/generate.js --version 7.58 --force",
    "generate:756": "node scripts/generate.js --version 7.56 --force",
    "generate:latest": "node scripts/generate.js --version latest --force",
    
    "// === COMPLETE PIPELINE ===": "",
    "pipeline": "npm run scrape && npm run generate",
    "pipeline:all": "npm run scrape:all && npm run generate:all",
    "pipeline:758": "npm run scrape:758 && npm run generate:758"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.1.2",
    "turndown": "^7.2.0"
  }
}
